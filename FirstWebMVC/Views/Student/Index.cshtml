@model IEnumerable<FirstWebMVC.Models.Student.Student>
<h2>Danh sach Sinh vien</h2>
<table class="table table-bordered">
    <tr>
        <th>Ma SV</th>
        <th>Ho Ten SV</th>
        <th>Actions</th>
    </tr>
    
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.StudentCode</td>
            <td>@item.FullName</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StudentCode" class="btn btn-warning">Edit</a>
                <a asp-action="Delete" asp-route-id="@item.StudentCode" class="btn btn-danger">Delete</a>
            </td>
        </tr>
    }
</table>

@* @section Styles{
    <link rel="stylesheet" href="~/css/student.css  "/>
}

@if(ViewBag.Error == true){
    <p class="text-danger">@ViewBag.Message</p>
}
else{
    <p class="text-dark">@ViewBag.Message</p>;
}

<form asp-action="Index" method="post">
    <div class="mb-3">
        <div> 
            Mã sinh viên: 
            <input type="text" class="form-control w-25" name="StudentCode" value="@ViewBag.StudentCode" placeholder="Nhập mã sinh viên của bạn"/> <br />
        </div>
         <div> 
            Họ và tên: 
            <input type="text" class="form-control w-25" name="FullName" value="@ViewBag.FullName" placeholder="Nhập họ tên của bạn" /> <br />
        </div>
        <div>
            <button type="submit" class="btn btn-secondary">Gửi dữ liệu</button> <br />
        </div>
    </div>
</form> *@






